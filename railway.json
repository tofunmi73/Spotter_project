{
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "cd spotter_trip_app_backend && pip install -r requirements.txt"
    },
    "deploy": {
      "startCommand": "cd spotter_trip_app_backend && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn spotter_trip_app.wsgi",
      "restartPolicyType": "ON_FAILURE",
      "restartPolicyMaxRetries": 10
    }
  }